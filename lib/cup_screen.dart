import 'package:flutter/material.dart';

class CupScreen extends StatelessWidget {
  const CupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Cup Screen'),
      ),
    );
  }
}
